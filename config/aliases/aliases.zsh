(($+commands[nvim])) && alias vim="nvim" vimdiff="nvim -d"

# Verbosity
alias cp="cp -iv"
alias mv="mv -iv"
alias rm="rm -v"
alias mkd="mkdir -pv"
alias bws="bw-search"
alias k="kubectl"
alias v="nvim"
alias ls="eza"
alias pdf="atril"

# Colors
# alias ls="ls -hN --color=auto --group-directories-first"
alias grep="grep --color=auto"
alias diff="diff --color=auto"

# Assuming an AWS PRofile
alias asm="source asm.sh"

# Bitwarden Search
alias bws="bw-search"
